# Core Django
Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0
Pillow==10.3.0
python-decouple==3.8

# MongoDB - Official Django MongoDB backend
pymongo==4.10.1
django-mongodb-backend==5.2.4

# Production server
gunicorn==22.0.0
whitenoise==6.7.0

# Crop Recommendation Model (scikit-learn RandomForest)
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2

# Plant Disease Detection (Hugging Face MobileNetV2 - loaded directly via PyTorch)
# torch and torchvision are already installed on this machine via pytorch.org
# transformers is used only for MobileNetV2ForImageClassification model loading
transformers>=4.41.2
torch>=2.3.0
torchvision>=0.18.0
django-ratelimit==4.1.0
